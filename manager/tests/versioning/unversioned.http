PUT /apps/thor/versions/unversioned

{
    "schema": {
        "type": "object",
        "properties": {
            "nickname": {
                "type": "string"
            }
        },
        "required": [
            "nickname"
        ]
    },
    "configuration": {
        "nickname": "Thor"
    }
}

###
GET /apps/thor/versions/unversioned/configuration

###
PUT /apps/thor/versions/unversioned/configuration

{
    "nickname": "Tornado"
}

###
PUT /apps/thor/versions/unversioned

{
    "schema": {
        "type": "object",
        "properties": {
            "nickname": {
                "type": "string"
            },
            "age" : {
                "type": "decimal"
            }
        },
        "required": [
            "nickname",
            "age"
        ]
    },
    "configuration": {
        "nickname": "Thor",
        "age": 0.8
    }
}

###
GET /apps/thor/versions/latest/configuration

###
GET /apps/thor/versions/latest

###
PUT /apps/thor/versions/unversioned

{
    "schema": {
        "type": "object",
        "properties": {
            "nickname": {
                "type": "string"
            },
            "age" : {
                "type": "decimal"
            }
        },
        "required": [
            "nickname",
            "age"
        ]
    },
    "configuration": {
        "nickname": "Thor",
        "age": 0.8
    }
}

